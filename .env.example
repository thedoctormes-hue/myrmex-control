# Myrmex Control Environment Variables
# Copy to .env and run POST /api/setup to create password hash
# The setup endpoint creates MYRMEX_PASSWORD_HASH automatically

# CORS origin (comma-separated for multiple)
CORS_ORIGIN=http://localhost:5173

# Node environment
NODE_ENV=development

# Port for server
PORT=3000